name: CI-CD Pipeline

on:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3

      - name: Build Docker images
        run: |
          docker build -t pedidos ./pedidos
          docker build -t pagamentos ./pagamentos
          docker build -t estoque ./estoque
          docker build -t gateway ./gateway

      - name: Run basic tests
        run: echo "Testes executados com sucesso"

      - name: Login DockerHub
        run: echo "${{ secrets.DOCKER_PASSWORD }}" | docker login -u "${{ secrets.DOCKER_USER }}" --password-stdin

      - name: Tag images
        run: |
          docker tag pedidos ${{ secrets.DOCKER_USER }}/pedidos:latest
          docker tag pagamentos ${{ secrets.DOCKER_USER }}/pagamentos:latest
          docker tag estoque ${{ secrets.DOCKER_USER }}/estoque:latest
          docker tag gateway ${{ secrets.DOCKER_USER }}/gateway:latest

      - name: Push images
        run: |
          docker push ${{ secrets.DOCKER_USER }}/pedidos:latest
          docker push ${{ secrets.DOCKER_USER }}/pagamentos:latest
          docker push ${{ secrets.DOCKER_USER }}/estoque:latest
          docker push ${{ secrets.DOCKER_USER }}/gateway:latest